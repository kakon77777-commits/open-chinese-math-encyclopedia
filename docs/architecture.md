# MVP 架構

```text
Canonical MKO JSON
├── Vite + KaTeX 人類閱讀介面
├── MCP Server AI 介面
├── JSONL 批次匯出
├── Python 計算伴隨
└── FELRA 專案規格
```

核心原則：

$$
\text{數學}\neq\text{程式}\neq\text{有限計算證據}\neq\text{形式證明}
$$

本 MVP 僅打通一個垂直切片；新增條目時，不得把 HTML 當作唯一知識來源。
